#include <stdio.h>

struct app {
    int n;
    char registro[n];
    int amigo[n] 
};

int main()
{
    int n;
    scanf("%d", &n);
    int a1[n];
    char a2[n];

    for (int i = 0; i < n; i++) 
    {
        scanf(" %c %d", &registro[i], &amigos[i]);
        
    }
    for (int i = 0; i < n; i++)
    {
        if (a2[i] == 'T')
        {

        }
    }

    
    return 0;
}